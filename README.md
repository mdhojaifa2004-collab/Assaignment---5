📂 GitHub Repository
Create a beautiful GitHub Readme with the following description:

1. Name of your project
  "Dev Stack"

2. A little description
  "Dev Stack Builder is a modern platform that helps developers explore popular technologies and build their own development stack. Easily discover tools, compare technologies, and add your favorites to your personal stack."

3. Technology that you use
  "1. React.js
  2. TypeScript
  3. Tailwind CSS
  4. DaisyUI
  5. React-Toastify
  6. JSON
  7. Vite"

4. 3 features about your project
"1. Technology Explorer – Explore popular technologies.
2. Stack Builder – Build your own tech stack.
3. Responsive UI – Works on all devices."





**** Also answer these React questions at the end of your Readme (write the   answers in your own words, short and simple):



1. What is JSX, and why is it used in React?
  Ans. "JSX হলো JavaScript-এর একটি syntax, যার মাধ্যমে JavaScript-এর ভিতরে HTML-এর মতো code লেখা যায়। React-এ UI সহজে তৈরি করার জন্য JSX ব্যবহার করা হয়।"

2. What is the difference between props and state?
  Ans. "Props দিয়ে parent component থেকে child component-এ data পাঠানো হয়। আর State হলো component এর নিজের data, যা পরিবর্তন হতে পারে।"


3. What does the useState hook do, and where did you use it in this project?

  Ans. "useState component-এর data store এবং update করতে ব্যবহার করা হয়। এই project-এ আমি selected technologies-এর stack সংরক্ষণ করতে useState ব্যবহার করেছি।"


4. What does the useEffect hook do, and why did you need it to load the       JSON    data?
  Ans. "useEffect component render হওয়ার পর side effect চালানোর জন্য ব্যবহার করা হয়। JSON data load করার মতো কাজের জন্য এটি ব্যবহার করা যায়।"
  (তবে ‍আমি আমার এটি ব্যবহার করি নি) ;


5. Why does every item in a .map() list need a unique key prop?
  Ans."React যেন প্রতিটি item আলাদাভাবে চিনতে এবং list-এর পরিবর্তন ঠিকভাবে update করতে পারে, এজন্য unique key ব্যবহার করা হয়।"


6. What is conditional rendering? Show one place you used it (example: the 
 empty stack message).
 Ans. "condition-এর উপর ভিত্তি করে নির্দিষ্ট UI দেখানো বা না দেখানোকে conditional rendering বলে। আমি Your Stack-এ stack empty থাকলে “Your stack is empty” message দেখানোর জন্য এটি ব্যবহার করেছি।"


7. How do you pass data from a parent component to a child component, and how  does a child send something back to the parent?
Ans. "Props-এর মাধ্যমে parent থেকে child-এ data পাঠানো হয়।"

<!-- ------------------------------------------------------------------------------------------------------------------------------------------------------------ -->